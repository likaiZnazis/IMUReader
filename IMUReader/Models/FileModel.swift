import SwiftUI

// Here we have both possible ways for storing files
var csvFileURL: URL?
// Probably will later add a way to export JSON file
// With JSON format I could segment the repetitions better
var jsonFileURL: URL?
